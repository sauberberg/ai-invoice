streamlit==1.25.0          # Основной фреймворк для веб-приложения
python-dotenv==1.0.0       # Загрузка переменных окружения из .env
openai==1.0.0              # Официальная библиотека OpenAI API (>=1.0 для ChatCompletion)
langchain==0.0.240         # LangChain для работы с LLM (включая поддержку ChatOpenAI)
langchain-openai==0.3.19   # Интеграция LangChain с OpenAI (ChatOpenAI, если требуется)
PyMuPDF==1.22.5            # Библиотека для извлечения текста из PDF (fitz)
pdfplumber==0.9.0          # Альтернатива для извлечения текста из PDF (pdfminer.six)
pytesseract==0.3.10        # Python-обёртка для Tesseract OCR
Pillow==9.5.0              # Библиотека для обработки изображений (необходима для pytesseract)
pydantic==2.3.0            # Pydantic для валидации (непосредственно нужен LangChain)